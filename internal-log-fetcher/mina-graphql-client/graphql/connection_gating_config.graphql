query ConnectionGatingConfigQuery {
    connectionGatingConfig {
        trustedPeers {
            peerId
            host
            libp2pPort
        }
        bannedPeers {
            peerId
            host
            libp2pPort
        }
        isolate
    }
}
